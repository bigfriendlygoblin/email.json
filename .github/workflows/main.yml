name: Email Step Workflow

on:
  push:
    branches:
      - main  # or your default branch

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: 24f2001830@ds.study.iitm.ac.in
        run: echo "Hello, world!"
